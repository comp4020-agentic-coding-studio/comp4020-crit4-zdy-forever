node_modules/
dist/
*.log

# Auditioning renders from tools/violin-lab: regenerate with
# `node tools/violin-lab/render.mjs`. The candidates that produce them are
# tracked; 1.6 MB of WAV per candidate is not.
tools/violin-lab/out/

# Screenshots handed over for debugging a live session -- useful in the
# conversation, not part of the prototype.
screenshot/

# editor and OS junk
.DS_Store
._*
Thumbs.db
Desktop.ini

# keep secrets out of the repo: your API key lives in an env var or an
# untracked file, never in a tracked one
.env
.env.*
.envrc
mise.local.toml
*.pem

# Claude Code's project directory is machine-local: settings in here can hold
# your course API key, and this repo goes public when you ship. Your tracked
# harness is CLAUDE.md (and any AGENTS.md), not this directory.
.claude/
